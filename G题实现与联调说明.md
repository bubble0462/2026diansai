# G 题周期信号测量分析装置 - 当前实现与联调说明

## 已实现的软件功能

- ADC1 由 TIM2 TRGO 触发，采样率为 2 MHz，DMA 一次采集 4096 点。
- 频谱频点间隔为 `2 MHz / 4096 = 488.28125 Hz`，满足 500 Hz 分辨率要求。
- Hann 窗 FFT、对数抛物线频率插值，测量范围 10 kHz 至 500 kHz。
- 自动识别基波及最多两个谐波，显示各分量频率和峰值幅度。
- 由识别出的频率分量重构被测信号，计算真有效值和峰峰值。
- 显示时域波形、正频率频谱、Upp、Urms、基频和各谱线参数。
- KEY0 在一屏 1 个完整周期和 3 个完整周期之间切换。

## 必须配套的模拟前端

题目信号为 50 欧姆源输出的 50 mV 至 250 mV 双极性小信号，不能直接接入
STM32 的 0 至 3.3 V ADC。建议输入链路如下：

`BNC -> 50Ω端接/高阻切换 -> 过压保护 -> 低噪声可变增益 -> 1.65V偏置
-> 约700kHz低通 -> ADC1_IN1(PA1)`

第 3 项要求抑制不低于 1 MHz、200 mVpp 的单频干扰，必须主要由模拟低通完成。
数字重构只能清除已被 ADC 正确采集且没有混叠的带外分量，不能逆转 ADC 前发生的
混叠或削顶。建议低通在 500 kHz 内幅频误差足够小，并在 1 MHz 处提供足够衰减。

## 幅值标定

`BSP/My_ADC/ad.h` 中的 `AD_INPUT_MV_PER_COUNT` 是输入端每 ADC 码对应的毫伏数。
默认值等同于无增益直连，仅用于跑通程序。完成模拟前端后，用校准信号逐档测量，
把前端实际增益计入该常数；如采用多档 PGA，应改为运行时按增益档选择标定系数。

至少使用 50 mVpp、100 mVpp、250 mVpp，在 10 kHz、200 kHz、500 kHz 三个频点
做九点标定和误差验证。幅值误差目标应留出裕量，建议台架调到 3 mV 以内。

## 当前硬件限制

- 工程驱动的是 240x320、3.2 英寸 ST7789VW；题面明确要求显示屏不小于 6 英寸。
  正式作品必须更换合规显示屏或增加合规的外接显示模块。
- 现有仓库没有模拟前端原理图，不能仅凭固件保证 ±5 mV 的幅值指标和第 3 项抗干扰。
- 上板前应确认系统时钟保持当前 144 MHz 配置：APB1 定时器时钟 72 MHz，
  TIM2 的 ARR=35 才对应 2 MHz。若改回 168 MHz，必须同步重算 TIM2。

## 操作

- 上电后自动连续测量并刷新。
- KEY0：切换显示 1T / 3T。
- 屏幕参数中 `A` 是题目所述正弦频谱分量峰值，不是峰峰值。
