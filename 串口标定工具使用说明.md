# 串口标定采集工具

将压缩包完整解压到同一个文件夹，然后双击 `启动串口标定.bat` 打开界面。
不要直接在压缩包预览窗口中运行。

电脑需要安装 Python 3.10 或更高版本。启动脚本会检查 `pyserial`、`openpyxl`
依赖；缺失时会自动通过 pip 安装。保持 `calibration_logger.py` 和 `拟合.xlsx`
与启动脚本位于同一目录。

## 接线

- USB 转串口 RX 接 STM32 PA2 / USART2_TX。
- USB 转串口 TX 接 STM32 PA3 / USART2_RX（当前工具只接收，PA3可暂不接）。
- USB 转串口 GND 与板卡 GND 相连。
- 串口参数固定为 115200、8 数据位、无校验、1 停止位。
- 串口屏继续独占 USART1 的 PA9/PA10，不需要拔除。

## 操作

1. 选择桌面的 `拟合.xlsx`。
2. 选择 COM 口并连接。
3. 在页面中央填写信号源当前输出的频率（kHz）和峰峰值（mVpp）。
   标定频率为10～500kHz、步进10kHz；幅度为50～250mVpp、步进50mVpp。
4. 等信号稳定后，点击“自动采集并一次性写入当前行”。
5. 工具根据输入的频率和幅度匹配 `标定数据` 对应行，自动连续读取3帧新的
   频率、Vpp、RMS；3帧全部通过频率检查后才一次性写入。
6. Excel 自动计算 Vpp/RMS 的均值和样本标准差，页面也会立即显示本批统计结果。
7. 如果当前行已有1帧或2帧，点击一次只补采剩余帧，不覆盖原数据。
8. 达到设定次数后自动切换下一点。

Excel 可以保持打开。工具会优先写入当前打开的工作簿并保存；如果 Excel 没有打开，
则直接安全更新文件。若串口实测频率与当前标定点相差超过 2 kHz，工具拒绝写入。
若输入的频率/幅度组合在 Excel 中没有对应行，工具也不会写入。

固件通过独立 USART2 每约 500ms 输出一行：

```text
CAL,F=100.001,VPP=247.50,RMS=87.51
```

工具只解析这种 `CAL` 数据，不会干扰 USART1 串口屏通信。
